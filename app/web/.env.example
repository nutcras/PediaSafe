# How the Next.js server reaches the Hono API (server-side fetch + /api proxy
# rewrite target). The browser calls relative /api/* and Next proxies it here.
#   Docker:    http://pediasafe-api-server:3000
#   Local dev: http://localhost:3000
INTERNAL_API_URL=http://localhost:3000
